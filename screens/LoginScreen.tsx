import React from "react";
import { useNavigation } from "@react-navigation/native";
import { Text } from "react-native";


const LoginScreen = () => {
    const navigation: any = useNavigation();
    return (
        <Text>LoginScreenStart</Text>
    )
};

export default LoginScreen;